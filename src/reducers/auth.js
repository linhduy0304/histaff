
import {
  LOGIN_REQUEST,
  LOGIN_SUCC,
  LOGIN_FAIL,
} from '../config/types';

const initialState = {
  auth: null,
}

export default function auth (state = initialState, action) {
  switch(action.type) {
    
    default: 
      return state
  }
}